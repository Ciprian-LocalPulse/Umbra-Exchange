[package]
name = "reputation-accumulator"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Aggregates verified proof-of-observation submissions into per-indicator confidence scores, per docs/PROTOCOL_SPEC.md §5."

[dependencies]
